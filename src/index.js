import React from 'react';
import ReactDOM from 'react-dom/client';
import DCUConnect from '../App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <DCUConnect />
  </React.StrictMode>
);
